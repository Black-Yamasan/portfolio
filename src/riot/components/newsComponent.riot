<news-component>
  <section class="l-section">
    <h2 class="o-heading u-ta-c u-mb-60">News</h2>
    <div class="l-content">
      <ul class="c-news">
        <li class="c-news__child" each={ (item, i) in state.items }>
          <a href="{ item.url }" class="c-news__link u-text-link" target="_blank">{ item.title }</a>
        </li>
      </ul>
    </div>
  </section>

  <script>
    const KEY = 'Basic Zm5BRHBYcGMzbkFDRThkY29VRm9FVXpWYWJ3Ti1iOVlnVGQxUElVYTpuZXdzOnNlcnZlcg==';

    export default {
      state: {
        items: []
      },
      // onBeforeMount() {
      //   fetch('https://graphql.fauna.com/graphql', {
      //     method: 'POST',
      //     headers: {
      //       'Content-Type': 'application/json',
      //       'Accept': 'application/json',
      //       Authorization: KEY
      //     },
      //     body: JSON.stringify({query: "{ allLists(_size: 3){data {title url}} }"})
      //   })
      //   .then(r => r.json())
      //   .then((res) => {
      //     console.log(res)
      //     this.update({
      //       items: res.data.allLists.data
      //     })
      //   });
      // },
    }
  </script>

  <style>
    :host {
      display: block;
    }

    .c-news {
      display: flex;
      flex-wrap: wrap;
      justify-content: center;
      flex-direction: column;
      align-items: center;
    }

    @media screen and ( max-width: 768px ) {
      .c-news {
        display: block;
      }
    }

    .c-news__child {
      margin-bottom: 1em;
      font-size: 1.4rem;
      line-height: 1.5;
    }

    .c-news__child:last-of-type {
      margin-bottom: 0;
    }
  </style>
</news-component>